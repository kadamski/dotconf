[color]
    ui = auto
[push]
    default = simple
[alias]
    glog = log --oneline --graph --all --color --decorate
; [column]
;     ui = auto
[branch]
    sort = -committerdate
[core]
    excludesfile = ~/dotconf/gitignore_global
[include]
    path = ~/.gitconfig-local

[includeIf "gitdir:~/dotconf/"]
    path = ~/dotconf/gitconfig-priv

[includeIf "gitdir:~/NOTES/"]
    path = ~/dotconf/gitconfig-priv

[includeIf "gitdir:~/DEB/SID/"]
    path = ~/dotconf/gitconfig-priv

[commit]
	verbose = true
